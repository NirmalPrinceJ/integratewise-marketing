# Cloudflare Pages routing
# NOTE: Functions at functions/blog/* and functions/api/* are served automatically
# Only the SPA fallback is needed here for the main app
# DO NOT add /blog/* redirects — they override Functions

# SPA fallback for main app only (not blog, not api)
/index.html /index.html 200
