/**
 * Sanity Studio config for integratewise-marketing.
 * Deploy studio to marketing.integratewise.ai or sanity.io managed URL.
 *
 * Run: pnpm exec sanity init (if adding full studio)
 * Or: use Sanity's hosted studio at sanity.io/manage
 */
export default {
  // projectId and dataset come from .env
  // schema: { types: schemaTypes },
};
